class UserImage extends StatelessWidget {
  const UserImage(this.userImage, {Key? key}) : super(key: key);

  final String userImage;
  

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
